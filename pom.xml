<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- Defines the Jenkins plugin parent and therefore the supported Jenkins version line. -->
    <parent>
        <groupId>org.jenkins-ci.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>6.2221.va_045130417c9</version>
        <relativePath/>
    </parent>
    <groupId>io.jenkins.plugins</groupId>
    <artifactId>lark-notice</artifactId>
    <version>${revision}</version>
    <packaging>hpi</packaging>
    <properties>
        <revision>2.1.10</revision>
        <!-- https://www.jenkins.io/doc/developer/plugin-development/choosing-jenkins-baseline/ -->
        <jenkins.baseline>2.555</jenkins.baseline>
        <jenkins.version>${jenkins.baseline}.3</jenkins.version>
        <lombok.version>1.18.46</lombok.version>
    </properties>

    <name>${project.artifactId}</name>
    <description>Lark通知插件</description>
    <url>https://github.com/jenkinsci/lark-notice-plugin</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>xm</id>
            <name>xm.z</name>
            <email>721806280@qq.com</email>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git@github.com:jenkinsci/lark-notice-plugin.git</connection>
        <developerConnection>scm:git:git@github.com:jenkinsci/lark-notice-plugin.git</developerConnection>
        <url>${project.url}</url>
        <tag>${project.name}-${project.version}</tag>
    </scm>

    <dependencies>
        <!-- Jackson binding used to build and parse the webhook payloads. -->
        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>jackson2-api</artifactId>
        </dependency>
        <!-- Pipeline step API plus the StepListener hook the notifier registers. -->
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-api</artifactId>
        </dependency>
        <!-- BranchJobProperty, so multibranch jobs can carry their own robot config. -->
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-multibranch</artifactId>
        </dependency>
        <!-- Annotation processor only; never bundled into the hpi. -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
        <!-- Declarative pipeline {} syntax, for hpi:run and JenkinsRule tests only. -->
        <dependency>
            <groupId>org.jenkinsci.plugins</groupId>
            <artifactId>pipeline-model-definition</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Generic steps such as echo and withEnv, for hpi:run and JenkinsRule tests only. -->
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-basic-steps</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Agent-bound steps node, sh and bat, for hpi:run and JenkinsRule tests only. -->
        <dependency>
            <groupId>org.jenkins-ci.plugins.workflow</groupId>
            <artifactId>workflow-durable-task-step</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.jenkins.tools.bom</groupId>
                <artifactId>bom-${jenkins.baseline}.x</artifactId>
                <version>6931.vea_a_b_c6fd017d</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/releases/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>repo.jenkins-ci.org</id>
            <url>https://repo.jenkins-ci.org/releases/</url>
        </pluginRepository>
    </pluginRepositories>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <compilerArgs combine.children="append">
                        <arg>-Xlint:deprecation,unchecked</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/ConfigurationPageRenderTest.java</exclude>
                        <exclude>**/LarkManagementLinkTest.java</exclude>
                        <exclude>**/NotifierConfigDescriptorDefaultsTest.java</exclude>
                        <exclude>**/MessageSenderRegistryCacheInvalidationTest.java</exclude>
                        <exclude>**/NotificationServiceLayerTest.java</exclude>
                        <exclude>**/NotifierIntegrationTest.java</exclude>
                        <exclude>**/InjectedTest.java</exclude>
                        <exclude>**/LarkStepTest.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>jenkins-rule-tests</id>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/ConfigurationPageRenderTest.java</include>
                                        <include>**/LarkManagementLinkTest.java</include>
                                        <include>**/NotifierConfigDescriptorDefaultsTest.java</include>
                                        <include>**/MessageSenderRegistryCacheInvalidationTest.java</include>
                                        <include>**/NotificationServiceLayerTest.java</include>
                                        <include>**/NotifierIntegrationTest.java</include>
                                        <include>**/InjectedTest.java</include>
                                        <include>**/LarkStepTest.java</include>
                                    </includes>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
